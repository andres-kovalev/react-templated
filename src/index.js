const Slot = require('./components/Slot');
const Templated = require('./components/Templated');
const withSlots = require('./components/withSlots');
const Content = require('./components/Content');

module.exports = {
    Slot,
    Templated,
    withSlots,
    Content
};
